// Temporary stub file to resolve build errors
// This file exists only to prevent import errors during the transition from Supabase
// All Supabase functionality has been replaced with JWT authentication

export const supabase = {
  // This is a stub - all authentication is now handled by the JWT system
};

export default supabase;